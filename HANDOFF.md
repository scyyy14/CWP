# CWP 代码改进交接文档

这份文档给接手本项目的 AI 模型。用户要求继续实际改进代码，而非只整理文献或提出建议。先核对当前代码及实验文件，再实现、验证。无需依赖此前聊天即可开展工作。

## 1. 用户目标和必须遵守的边界

- 提高复杂、大规模 CWP 输入找到最短完工时间排程的概率。
- 常规命令行运行，包括求解、校验、输出及绘图，目标在 5 分钟内结束；等待用户关闭 GUI 窗口不计入计算耗时。
- **禁止第三方优化求解器**：不能调用 OR-Tools、Gurobi、CPLEX、SCIP、PuLP 等。现有算法是自行实现的 Python 搜索；Matplotlib 只绘图。
- 用户希望说明简单、清楚，并重视实际代码和同预算实验。
- 不要将“找到合法解”“接近下界”“达到已知最好解”写成“证明最优”。此前用户已多次指出改进未完成，务必如实报告。
- 本次交接仅写文档；下文列为“待实现”的功能尚未完成。

## 2. 仓库与工作区

- 根目录：`/Users/yy/Documents/ChatGPT/CWP`
- Python：`.venv/bin/python`
- 核心程序：`cwp_solver.py`
- 输入：`example_input.json`，目前已被用户扩大为 39 贝位、9 桥吊。不要改小输入来证明新算法有效。
- 测试：`tests/test_solver_improvements.py`
- 文献记录：`LITERATURE_NOTES.md`
- 模型与运行说明：`README.md`
- 当前大实例实验：`experiments/large39_review.md`、`experiments/large39/`、`experiments/large39_full/`
- 历史小规模实验：`experiments/20260912_review.md` 及同日期 JSON；这些不是当前输入结果。
- 实验工具：`tools/benchmark_solver.py`。注意它的 current 用例读取当前输入，所以历史比较不能不加说明地直接复跑并对比。
- 当前大规模改动之前的提交：`ba4560fc98aef2f598219f35f6d31b60c59e0981`。
- 写交接时工作区有未提交修改：核心程序、测试、README、文献记录，以及大规模实验产物。**这些是要保留的工作，不要 reset、checkout 覆盖或丢弃。** 接手先运行 `git status --short`、`git diff`。

## 3. 精确模型：不要套错论文假设

1. N 个贝位，编号 1..N；M 台同速桥吊，永久按物理位置从左到右编号。
2. W[i] 是贝位 i+1 的非负整数工作量。每台桥吊每时间槽最多完成 1 单位。
3. 原地作业：时间槽两端处于同一贝位，才能工作。
4. **移动任意距离都恰好占 1 个完整时间槽，本槽不能作业。** 不是按距离计时。
5. 原地空闲也合法。
6. 相邻桥吊在所有时间边界的贝位差至少为 2；所有位置必须位于 1..N，不能移出船舶边界停车。
7. 槽内采用线性移动，两端间距均 >=2 则全槽间距也 >=2，且不会交叉。
8. 同一贝位可以被多台桥吊在不同时间接续处理；可抢占、可拆分，没有额外换吊费用。不能同时工作。
9. 初始位置自由选择，但 S 中每个贝位必须在 t=0 的槽真正工作。因此相关桥吊在边界 t=0 和 t=1 都应位于该贝位。
10. 没有额外装卸先后关系、船体稳定性、异速桥吊、随机到达等约束。不要擅自新增，也不要放宽上述约束。

按词典序最小化：

`(makespan, split_bay_count, assignment_count, reversal_count, movement_count, load_deviation)`

第一目标永远是工期。后五项不能通过加权和牺牲更短工期。负荷目标权重为 `min(q+1, M-q)`（q 从 0 开始），位于中间的桥吊目标较重。

## 4. 当前难例与已验证结果

```json
{
  "W": [16,4,14,4,13,8,5,12,17,4,3,14,11,10,5,12,3,7,21,7,11,8,15,4,4,6,7,21,5,19,7,7,8,9,16,5,0,0,0],
  "M": 9,
  "S": [1,5,13,17,25,29]
}
```

- N=39，总工作量 342。
- 合法联合位置数 `C(N-M+1,M)=C(31,9)=20,160,075`。
- 当前有效工期下界 **41**，最好保存的合法工期 **44**。
- **最优值只知道在 [41,44]。并不知道 41 是否可行，也没有证明 44 最优。**
- 60 秒搜索预算：搜索 52.934 秒，工期 44；含首次绘图总时间 69.997 秒。
- 270 秒搜索预算：搜索 224.674 秒，仍是工期 44；轨迹修复改善工期 3 次，MCTS 与分层搜索均没有改善。此轮完整终端耗时未保留，不能将 search_seconds 当成含绘图总耗时。
- 两次输出均通过独立排程校验，见两个 large39 目录。
- 14 项自动测试通过，含小实例独立 BFS、初始化 DP 与完整枚举一致性、下界一致性、进程超时检查点等。测试数量和内容以当前文件为准。
- 没有对旧版 2016 万组合运行完整基准，避免将分析得到的复杂度差异误写为实测固定倍数提速。

## 5. 代码地图与当前执行过程

函数位置会随修改变化，用 `rg -n '^def ' cwp_solver.py` 定位。

| 函数/数据 | 功能与接手重点 |
|---|---|
| `Slot`、`Solution`、`_CandidateSchedule` | 标准结果结构；不要破坏 JSON 兼容性及词典序比较 |
| `main` | 300 秒总体截止目标；调用限时求解、校验、保存 JSON、独立绘图 |
| `solve_cwp_bounded`、`_solve_worker` | spawn 子进程，搜索最多 270 秒；每次更优完整方案校验后原子保存，超时取检查点 |
| `solve_cwp` | 搜索总控、各阶段预算、策略学习、结果证明状态 |
| `_validate_input` | 输入合法性、物理可达性、初始位置生成 |
| `_weighted_initial_positions` | 含强制贝位的路径图固定基数最大权独立集 DP；完整域最优初始覆盖 |
| `_makespan_lower_bound` | 六类有效下界；重要：完整覆盖不能用样本最大覆盖代替 |
| `_best_partition_owner_plan` | 连续分区种子；其他搜索仍允许跨分区共享和双向运行 |
| `_best_safe_dispatch`、`_construct_schedule` | 单槽加性评分 DP 选择安全位置，再逐槽构造；仅当前评分最优 |
| `_construct_schedule_legacy` | 少桥吊保留的旧聚焦组合解码器 |
| `_trajectory_repair` | 把完整方案目标工期减 1，进行区段替换及退火修复，已有多桥吊联动 |
| `_candidate_from_history` | 将轨迹转为逐槽作业、移动和空闲；多余原地容量不重复作业 |
| `_mcts_horizon_search` | 单个 incumbent 初始状态出发的部分排程树搜索，只改进，不证明 |
| `_bounded_layered_search` | 限宽分层及尾部重排，状态合并；截断不是精确证明 |
| `_exact_horizon_search` | 完整工期可行性搜索；只在真实联合位置数受控时启用 |
| `verify_solution` | 独立检查工作量、状态、位置连续、安全间距、强制首槽等 |

现有阶段预算：

- 有效预算 >=2 秒时，M>3 构造最多 min(5 秒, 总预算40%)；M<=3 最多 min(15 秒, 总预算90%)。这些截止从入口计时，会包含预处理。
- M<=3 前 300 轮用 DP，然后重置随机流与 UCB 学习，改用旧解码器；保存前一阶段最好解，最终不丢掉更优结果。
- 大于 3 台时有贝位优先级继承和变异；构造共 17 种评分规则，用 UCB 选策略。
- 当前总改进截止为预算95%；构造后剩余改进预算的65%分给轨迹修复。其后 MCTS 根据 M 与 gap 获得剩余的35%或55%，再分给四次分层重排。
- 达到下界后跳过更短工期搜索。构造中最多允许64轮连续无次级改进；并不总是立即停。
- 精确搜索只在 gap<=1 且**真实**组合数 <=20000 时启用。大实例不能因为初始候选只有125个就启用假精确证明。
- 直接 `solve_cwp()` 是协作式时间检查；硬中止靠 `solve_cwp_bounded()`。第一次完整构造仍有不传 deadline 的路径；不要宣传直接调用具有严格时间保证。

## 6. 已实现的大规模改动，勿重复当作待办

### A. 消除初始化联合位置爆炸

旧版完整枚举联合位置，并在下界每个窗口里再扫初始组合。新版真实组合 <=20000 保留完整枚举；更大时用 `_weighted_initial_positions` 和不同随机权重最多生成128个候选。本输入125个，预处理一次约0.077秒。

DP 的状态是在前 b 个贝位放 k 台桥吊，选/不选贝位，跳过相邻位置，并强制包含 S。单次 O(NM) 状态，Python 路径元组复制另有成本。该候选池只用于初始种子；单槽派工仍可用所有物理可达贝位。

### B. 下界仍覆盖完整域

`_makespan_lower_bound(..., starts)` 用该 DP 分别精确求总正作业、窗口、受限桥吊集合的最大初始覆盖。测试将完整枚举与一个初始候选的 DP 结果对照，保证截断不会抬高下界。

### C. 多桥吊联动区段修复

已有约25%的修复尝试允许邻吊联动。改变某吊一段位置后，向左/右传播最小必要让位，检查每个边界位置、船舶范围及 S 的首槽端点，并统计所有改变轨迹的工作容量增减。只在全部缺口为0时返回候选。

仅扩大这个算子的随机次数没有在270秒实验中突破44。不要把它重新命名后当成新的结构优化。

## 7. 本轮新增实现

- `solve_cwp` 现在维护最多16个结构不同的完整精英方案；按贝位归属和移动路线去重，普通轨迹修复会轮换这些来源。新增 `elite_pool_size`、`elite_repairs` 指标。
- `_critical_window_beam_repair` 选择最后完工桥吊附近的2～4台桥吊，在晚期窗口重建位置行，窗口外及其他桥吊轨迹固定。新增 `critical_repair_iterations`、`critical_repair_improvements` 指标。
- 当前小例12秒保持19；39/9大例20秒仍44，关键窗口没有改进。此轮算子已实现但尚未证明对大例有效。

## 8. 仍然存在的问题与待验证假设

以下是代码结构观察和实验推断，不是已完成的因果证明：

1. 只长期围绕一个最好完整方案修复，可能丢掉工期相同但初始位置、任务顺序不同的有潜力方案。
2. 大实例构造固定只有约5秒，当前多数规则仍处于少量尝试；可能未产生足够不同的优质分工结构。
3. 联动算子主要是最小让位，并不真正重新安排一串桥吊的任务访问顺序，可能越不过必须先退让再换序的障碍。
4. 修复归一为目标工期减1，缺口最好轨迹长期留存可能陷入局部最优；多重启并不等于多种完整初始结构。
5. MCTS 初始根通常来自一个 incumbent，聚焦候选在9台桥吊时很窄。本输入耗时后无改善，尚未做严格消融解释原因。
6. 分层搜索复制完整历史、层内瞬时状态集可能很大；时间展开也随工作量增长。初始化已缩放不代表整个算法已可扩展。
7. 阶段预算是固定比例/条件规则，不是真正按单位时间收益学习分配。
8. 有效下界41不一定可达。可能需要更紧的、经证明的下界，而不是永远追逐41。

## 9. 下一轮建议实施顺序

### P0：冻结基准和测量阶段收益

保留当前改动作为比较基线，记录代码版本或哈希、完整输入、种子、时间预算、首次可行解时间、每次工期改进时间、各算子耗时和贡献、最终合法性。增加观测不能明显拖慢每次迭代。

### P1：有界精英方案池与轮换修复

实现建议：

- 保存约8～16个完整合法方案，按工期优先筛选，再用初始位置、每吊访问顺序、贝位归属衡量差异；数量上限是起始建议，不是固定要求。
- 同工期不同结构不全被次级目标淘汰；可保留少量略差但差异大的方案用于搜索。最终输出仍严格使用既定词典序最好解。
- 轮换选择池中方案尝试 target=该方案工期-1 或当前全局最好工期-1；明确目标，避免修复到仍不改善全局的工期却误计收益。
- 修复调用分配短时间片，时间到返回控制权；较好的部分修复状态可按源方案保留，不在每次调度时完全丢弃。
- 每个新合法更优解走独立校验、原子检查点。池成员不能引用后来被修改的共享轨迹。

验收：同预算对照池大小1（接近当前）与多方案池；报告不同种子的实际工期，不只报告尝试数增多。

### P2：关键桥吊与阻塞链的局部重建

- 从真实最后作业时刻定位决定完工时间的桥吊，找出其等待/移动附近受到哪些邻吊位置约束。
- 选择连续2～4台桥吊和一个时间窗口，固定窗口之外的轨迹与其他桥吊，在窗口内重新分配剩余作业和访问顺序。
- 保留窗口左右端点、进出位置连续性，正确扣除窗口外已完成的工作；允许窗口内共享作业与双向移动。
- 可用自编束搜索、DP或有严格短截止的枚举子问题，不调用优化求解器。
- 分离“已有让位传播”和“重新决定访问顺序”的贡献，避免只写一个名字却没有新的邻域。

### P3：按实际收益分配预算

以完成一次修复/重排为调度单位，记录工期下降及花费时间，给有效算子更多份额，保留少量探索防止过早淘汰。保证剩余时间不足时仍及时返回当前合法结果。现有 UCB 派工规则选择不是这个跨阶段时间调度。

### P4：可证明的新下界或更紧容量判定

研究强制首槽、桥吊可达集合、移动次数、共享额外到达和局部安全容量的组合。先给数学论证，再用独立小实例精确枚举反证测试。不能根据未搜索到41推断下界42。

不要一次同时修改所有策略。每次合入一个可测量的结构改进，发现退化就分析、调整或保留旧策略作为对照。

## 10. 验证方法和建议用例

```sh
.venv/bin/python -m unittest discover -s tests -v
git diff --check
.venv/bin/python cwp_solver.py --time-limit 60 --seed 20260910 --no-show --output-dir experiments/next_large39_60
.venv/bin/python cwp_solver.py --time-limit 270 --seed 20260910 --no-show --output-dir experiments/next_large39_270
```

新建输出目录保留历史结果。需要比较时顺序跑求解器，避免并行抢CPU污染墙钟实验；记录整个 CLI 耗时和搜索耗时两项。相同随机种子在时间截断下不保证相同迭代数。

回归小例（历史有可行解达到有效下界）：

```json
{"W":[9,7,4,4,9,0,4,6,2,7,4,4,3,8,3,4,2,0],"M":5,"S":[5,10,12]}
```

最优工期19。

```json
{"W":[2,3,7,5,5,2,2,9,9,6,7,3,3,0,5,1,3,6],"M":3,"S":[2]}
```

最优工期31。

```json
{"W":[4,13,0,8,5,16,3,11,0,6,0,18,5,0,14,7,2,12,4,0,9],"M":3,"S":[2,6]}
```

最优工期50。此前全换DP导致51，隔离两类解码器学习后才恢复，勿无证据删旧分支。

至少增加不同布局的39/60贝位用例：工作集中左端/右端、中部拥挤、相邻重负荷、零工作区间、S密集、负荷尺度放大。未知最优的用例报告 UB/LB 和合法性，不声称最优。16个随机小例或3个种子不足以证明普遍命中概率。

## 11. 文献入口及适用边界

- Sampaio、Urrutia、Oppen，分解方法：https://arxiv.org/pdf/1604.00527 。路径与干扰调度分层；原方法使用数学规划、移动按距离计时。
- Sammarra 等，禁忌搜索与析取图局部搜索：https://iris.unical.it/handle/20.500.11770/129970 。此前可访问作者机构摘要，不能声称完整复现。
- Lu 等，可抢占连续贝区启发式：https://cedric.cnam.fr/~bentzc/INITREC/Files/CB2.pdf 。与本模型接近，但旅行时间不同。
- Huang、Li，两级DP与工作量共享：https://dr.ntu.edu.sg/server/api/core/bitstreams/5d477030-556b-47db-b055-21e548b3fcfa/content 。有单向、连续分区和船外让位等假设，不能直接套用。
- Jooken 等，MCTS论文相关作者实现：https://github.com/JorikJooken/MCTSQuayCraneSchedulingNonCrossingConstraints 。时间/束宽控制可借鉴，先核对模型，不能直接引用其基准最优值。
- Park、Bae，ML代理与GA：https://arxiv.org/abs/2411.03324 。目前只核对摘要，本项目没有对应训练数据，尚未引入机器学习。

继续检索时优先作者全文、正式出版页和作者代码。说明借鉴思想和实际实现之间的区别，不要堆论文名称代替设计与实验。

## 12. 接手后的交付标准

1. 说明实际修改哪些搜索结构，哪些尚未做。
2. 保存源代码、必要的回归测试、可复现命令、输入与结果记录。
3. 与本轮44工期进行同预算比较；若仍44，明确说没有观察到质量提升，不用初始化加速替代工期改进证据。
4. 只有找到43/42/41并通过独立校验，才报告相应工期改善。只有有效下界达到同值或完整证明，才报告最优。
5. 不承诺任意大规模输入都能在五分钟内找到或证明全局最优；继续朝提高实际命中率与控制运行时间的方向工作。
