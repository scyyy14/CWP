# 给 Luna max 的 CWP 改进交接方案

日期：2026-09-12。用途：直接执行代码改进，不需要此前聊天。文档生成时下面的修复尚未实施；本次执行已完成主要修复，结果见 `experiments/luna_max_20260912/report.md`。本文件优先于旧 `HANDOFF.md` 中关于“待实现精英池/关键窗口”的描述：这两个功能已经存在，不能再以添加同名功能作为完成标准。

## 1. 目标、基线与边界

用户目标：提高大输入找到最短完工时间方案的概率，单次正常 CLI 求解、校验、输出及绘图总时间不超过 300 秒，不使用第三方优化求解器。不要承诺任意规模都能在五分钟内证明最优。

工作目录 `/Users/yy/Documents/ChatGPT/CWP`；运行时 `.venv/bin/python`。审阅时 HEAD 是 `6229e27dc432f1cc7bc53dff4a21aa87840c91f1`，未提交文件为 `example_input.json`、`output/schedule.json`、`output/schedule.png`。它们是用户工作，禁止覆盖或回滚。接手重新检查状态；实验另建目录。

当前难例必须固定存入独立测试夹具，不要通过缩小用户输入展示效果：

```json
{"W":[16,4,14,4,13,8,5,12,17,4,3,14,11,10,5,12,3,7,21,7,11,8,15,4,4,6,7,21,5,19,7,7,8,9,16,5,0,0,0],"M":9,"S":[1,5,13,17,25,29]}
```

39 贝位、9 桥吊、总工作量 342。完整安全位置空间有 C(31,9)=20,160,075 个状态，不适合每层全枚举。当前保存结果工期 44，下界 41，未证明最优。只能说最优值在 [41,44]，不能把 41 当成已知最优答案。

历史记录 `experiments/next_large39_review.md`：60 秒搜索预算、seed=20260910，工期 44，搜索 51.233 秒，程序 68.800 秒；精英池 16，elite_repairs=20，critical_repair_iterations=772633，critical_repair_improvements=0。此前同预算也是 44。新增功能没有显示工期收益。以上是已有记录，本轮没有重跑实验；“20 次精英修复”也不等于 20 次全局工期改善。

## 2. 不可改变的模型

- 贝位编号 1..N，内部桥吊编号 0..M-1，输出桥吊编号 1..M。桥吊永久从左到右排序。
- 每槽驻留才能做 1 单位工作；移动任意距离都恰好耗费 1 槽，本槽不能工作；允许空闲。
- 相邻位置差至少 2，不能交叉或移到船外。槽内线性移动，两端安全间距可以保证槽内安全。
- 工作可抢占、可由不同桥吊在不同时间接续；不允许同槽重复作业。没有额外先后关系、异速或换吊费用。
- 初始位置自由，但 S 中贝位在第 0 槽必须实际工作，因此对应桥吊的第 0、1 个位置边界均须固定。
- 严格按 `(makespan, split_bay_count, assignment_count, reversal_count, movement_count, load_deviation)` 词典序比较。不能用加权和牺牲工期。
- 禁止 OR-Tools、Gurobi、CPLEX、SCIP、PuLP 等第三方优化求解器；使用自己实现的 Python 搜索。保留现有校验与原子检查点。
- 只有可靠下界相等或完整精确搜索证明，才能宣称工期最优。束搜索截断、采样失败、超时都不能证明不可行。工期最优也不等于全部次目标最优。

## 3. 先理解已有实现，避免重复建设

核心是 `cwp_solver.py`。已存在初始位置动态规划、每槽安全位置动态规划、策略组合搜索、轨迹退火修复、精英池、关键窗口束搜索、MCTS、分层搜索和小规模精确搜索。

重点入口（行号会随编辑变化，以函数名为准）：

| 位置 | 作用 |
| --- | --- |
| `_trajectory_repair`，约 1131 行 | 缩短一个槽后修复缺失工作，可指定桥吊和窗口 |
| `_critical_repair_windows`，约 1565 行 | 选择末完工桥吊附近的链与窗口 |
| `_critical_window_beam_repair`，约 1610 行 | 束搜索重建局部位置轨迹 |
| `solve_cwp` 内 `remember_elite`，约 2104 行 | 精英去重、排序与淘汰 |
| `solve_cwp`，约 2369 行 | 关键窗口与精英来源的调度 |
| `solve_cwp_bounded`，约 2777 行 | 子进程求解与超时检查点返回 |

已有全域下界和精确搜索规模门槛不要改成“在采样位置集合内证明”。已有小桥吊构造兼容逻辑曾修复 50 退化到 51 的问题，不要无实验删除。

## 4. 已确认的问题：第一批必须修复

这些是代码事实；它们是否主导难例停在 44，需要消融实验确定。

1. **窗口定义与执行不一致。** 调度器选了 `window`，但束搜索函数没有这个参数，内部重新随机选窗。窗口列表从 `best` 生成一次，实际修复来源却轮换其他精英，源方案和窗口可能不匹配。应先选 source，再从该 source 选链和窗口，显式传入统一规格。
2. **局部修复首先做全局压缩。** 两种修复都会删除所有桥吊的一行位置，然后只允许少量桥吊/窗口修改。删除处可能不在窗口，造成无法在局部补回的工作缺口。“冻结窗口外轨迹”目前只相对于压缩后的轨迹成立，不是相对于原方案成立。需要重定义压缩的时间映射，不能只改注释。
3. **小 M 的链会越界。** 生成器固定尝试宽度 2、3、4，M<4 时可能产生非法编号；束搜索未过滤，存在索引错误风险。生成与消费两端检查，覆盖 M=1、2、3 和空窗口。
4. **束搜索内层缺少时间、内存控制。** deadline 只在时间层之间检查；每层展开所有状态与候选笛卡尔积后才截断，且复制完整历史。即使束宽 350，也可能产生很大的瞬时字典并超出算子时间片。
5. **束搜索失败深度处理错误。** `next_states` 为空时 break，随后仍拿上一深度 states 当作窗口末端状态，并从 window_end 消耗后缀。这会使剩余工作账目与实际深度不一致。最终解码有再次检查，但不能依赖它掩盖中间逻辑错误。未到完整窗口深度应丢弃该次尝试，或显式记录深度并正确接续。
6. **同结构更好精英被直接丢弃。** 签名只含贝位归属与移动顺序，不含时间；命中签名直接 return，没有替换更好版本。修复为同签名保留更小 objective_key，并确保全局最优始终在池中。更新最好工期后重新清理超出准入阈值的旧成员。
7. **时间片结束丢失部分修复进度。** `_trajectory_repair` 的 best_paths、best_loss 等是调用内局部状态，下一次换种子重来。困难修复可能反复回到起点。
8. **关键窗口测试可空通过。** `test_critical_window_repair_returns_only_valid_schedules` 只有返回非 None 才校验，算子永远失败也能通过。需要“已知存在改进且必须找到”的确定性小夹具。

第一批验收：以上问题有针对性测试；现有测试通过；不得以它们修复完成宣称已改善大实例工期。

## 5. 第二批：把窗口重排改成有明确边界的小问题

先实现可靠的同工期重排，再接入缩短工期。建议新增 `RepairWindow` 数据结构，包含 source_id、target_horizon、active_cranes、位置边界 a/b、冻结参考轨迹及时间映射；明确槽区间为 [a,b)，位置行是 a..b。

### 5.1 同工期重排

冻结窗口外和非活动桥吊的实际作业、轨迹；固定活动桥吊入口与出口位置。统计所有冻结作业的每贝位数量 F[i]，局部需求 R[i]=W[i]-F[i]。F 应来自冻结的实际 work 槽，不能误把空闲驻留也算成已经完成的工作。

只搜索局部 R，防止窗口抢做已预留给固定后缀的任务。局部状态至少含时间、活动位置和剩余需求。每个动作同时检查固定邻居边界、安全间距及第一槽要求；保留驻留和必要移动，不只按最大工作量取候选。

最小版本固定入口/出口，后续再显式扩大可变边界。重组时校验冻结的作业和位置是否确实保留。现有按位置贪心解码可能改变外部 work/idle 分配，若使用它，必须额外检查冻结契约，不能仅检查总工作量。

### 5.2 再做 H→H-1 的压缩

选择一个明确删除槽/边界，只在对应局部区域制造压缩，并为后缀给出明确平移映射。固定桥吊同样要在这个映射下重新统计 F，检查拼接边与工作缺口。删除操作必须属于所选窗口，不再独立随机删除另一处。

先做必要条件筛选：R 不得负；剩余总需求不能超过活动可用槽容量；每贝位需求不能超过允许桥吊在窗口中的最大可能驻留容量。容量上界必须乐观，不能因启发式估计过低而错误剪枝。局部无法弥补缺口时扩大链/窗口或换删除点，不反复耗时修复同一个明显无望的局部问题。

先争取 44→43，再以新最好解争取 42、41。来源工期为 46 时，修到 45 只是来源改善，不是突破当前 44；探索可以保留，但目标和奖励要区分。

### 5.3 局部搜索工程约束

- 先依据固定左右邻居缩小每台桥吊候选范围；递归生成满足顺序间距的联合位置，避免先做大笛卡尔积再大量拒绝。
- 每至多 128 次候选扩展检查 deadline；所有辅助排序/预计算也须有规模上限。每层瞬时状态数有硬上限，保留父指针，不为每个节点复制整段历史。
- 相同时间、位置、剩余需求去重；只使用有证明的支配规则。启发式裁剪明确标记为不完整搜索。
- 用一个独立的极小窗口穷举参考实现验证局部可行性。测试模式关闭束截断、遍历完整局部候选域，与参考结果一致。
- 非空成功夹具、确定失败夹具、冻结边界、首槽、工作守恒、失败层、超时中断都要覆盖。

## 6. 第三批：改善搜索结构，而非继续堆随机次数

### 6.1 可继续的修复状态

新增有界 `RepairState`，保存当前轨迹、最好缺口轨迹、计数、温度/迭代进度和 RNG 状态。时间片到时返回状态，下次继续；完整可行解和未完成状态分开。键包含输入摘要、source 摘要、目标工期、链、窗口、冻结边界版本，禁止把不同问题的部分轨迹混用。缓存设数量/内存上限；测试分段继续与不分段运行在固定扩展次数下的一致性。

### 6.2 真正按阻塞选择桥吊链

当前选择主要是末完工桥吊右侧连续 2～4 台，并未识别实际阻塞。检查晚期未完成贝位、桥吊移动/等待，以及左右邻居占位是否排除了有用动作；从两侧构造候选链。非工作槽只是候选线索，不能直接认定为阻塞。按可消除的缺口、移动/等待及窗口容量筛选。若小链失败，有预算时逐步扩大窗口或链，不能永远只处理最后一小段。

加入明确改变访问顺序的动作，例如窗口内两个作业块重排、相邻桥吊工作段转移、让位后返回；全部经过同一个安全解码器。不要只增加单段位置扰动次数。

### 6.3 精英池与预算

精英池使用稳定 ID，保证全局最好解；同签名优者替换。质量之外保留少量归属/访问顺序不同的成员，用明确距离或分组配额避免 16 个近似同解占满。多样性措施要通过消融决定，不默认越多越好。

日志按算子分别记录调用次数、耗时、扩展数、最好缺口、合法返回数、来源改善数、全局工期改善数、次目标改善数、首次达到各工期的时间。退火提案数和束状态数不要合并成一个无法比较的 iterations。

在这些日志基础上分配预算：给各算子小额试用时间，依据全局工期收益及缺口进展调整，保留最低探索份额。MCTS/全局束搜索在已有一次实验无收益，不能据此永久禁用，但也不应固定占用大额预算。构造期同样记录多样性和耗时后再调整，不盲目增加初始化数量。

## 7. 五分钟限制怎么验收

保留 `solve_cwp_bounded` 的进程隔离、检查点校验和超时回收。直接 `solve_cwp` 是协作式 deadline，初始构造存在不带 deadline 的路径，不能把直接函数测试当成严格五分钟证明。

以父进程单调时钟给整个 CLI 一个 300 秒绝对截止时间，求解最多 270 秒且受剩余预算约束，绘图最多 25 秒且同样受剩余预算约束，预留校验/序列化/回收时间。不能因加了 270 秒参数就宣称整体满足限制；记录正常 CLI 从启动到退出的实际墙钟时间。极端机器负载下不能作硬实时承诺。

用实际大例测试超时前返回合法检查点，用人为很小预算测试中断、子进程回收及绘图剩余预算。不要为了实验将搜索和绘图并行占满 CPU，污染前后对比。

## 8. 实验与交付清单

先固定本轮改前代码副本及 SHA256、输入副本及 SHA256，再修改。实验保存在新的 `experiments/luna_max_日期/`，输出 JSON 记录完整输入、代码版本、种子、预算、工期、下界、最优标志、各阶段时间、总墙钟、校验结果和中断原因。`tools/benchmark_solver.py` 当前调用直接函数且 current 随用户输入变化，需要补固定夹具及独立 CLI 墙钟测试。

顺序运行，不能同时跑竞争 CPU 的 before/after。先小测试筛错，再做以下同预算比较：

| 用例 | 设置与用途 |
| --- | --- |
| 本文 39 贝位难例 | before/after 各 60 秒搜索预算，种子 42、7、20260910；比较最好值、中位数、各工期命中数和首次命中时间 |
| 同一难例长预算 | before/after 各 270 秒搜索预算，优先 seed=20260910，再补另外两种子；实际 CLI 总墙钟不超过 300 秒 |
| 历史小实例 | 保持原先预算和种子，回归工期 19、31、50；核对原始实验输入，不能拿不同输入比较 |
| 独立极小实例 | 完整 BFS/穷举验证解合法性、下界不超过真实最优、局部重排不漏完整域内可行解 |
| 额外规模实例 | 固定生成种子，覆盖稀疏工作、集中重负荷、较多首槽约束和更多桥吊；报告全体结果，不挑最好案例 |

历史小实例固定输入：

```json
[
 {"W":[9,7,4,4,9,0,4,6,2,7,4,4,3,8,3,4,2,0],"M":5,"S":[5,10,12]},
 {"W":[2,3,7,5,5,2,2,9,9,6,7,3,3,0,5,1,3,6],"M":3,"S":[2]},
 {"W":[4,13,0,8,5,16,3,11,0,6,0,18,5,0,14,7,2,12,4,0,9],"M":3,"S":[2,6]}
]
```

保留开关做消融：正确性修复基线 → 新局部重排 → 持续修复状态 → 阻塞链/访问顺序动作 → 自适应预算。先用 60 秒筛选，再对有效组合长测。不要一口气合入全部修改后无法解释哪个有效。

完成报告必须分开写：

1. 已修复哪些缺陷、哪些测试验证。
2. 工期是否真正突破 44；若得到 43，附保存方案和独立校验，不宣称最优。
3. 同预算多个种子的结果，以及实际 CLI 是否满足 300 秒。
4. 未完成项和失败实验。如果仍是 44，明确写“未突破当前难例”，继续定位缺口和受限邻域，不能用更多迭代、精英数量或次目标改善替代工期结果。

## 9. 建议给 Luna max 的执行指令

> 请读取 HANDOFF_LUNA_MAX.md，按第 4 节正确性修复、第 5 节局部问题重构、第 6 节搜索增强的顺序实际修改代码，并完成第 7、8 节验收。保留用户输入和现有输出，不使用第三方求解器。每阶段留下可复现实验和简短进度记录；阶段之间继续执行，不以只修复崩溃或新增同名功能作为任务完成。优先争取当前 39 贝位、9 桥吊难例从 44 降到 43，同时守住单次 CLI 五分钟预算。未达到目标如实报告，不保证未知最优值为 41，不把启发式失败当成不可行证明。
