# CWP 相关论文阅读与算法改进记录

## 2026-09-12 补充阅读与实际实现

本节描述当前版本；下文旧实验属于历史记录，不能用其“当前示例”字样解释现有输入。

- **Sammarra、Cordeau、Laporte、Monaco (2007), A Tabu Search heuristic for the Quay Crane Scheduling Problem**。[作者机构记录与摘要](https://iris.unical.it/handle/20.500.11770/129970)。阅读到的摘要将路径选择和调度分开，并用局部搜索缩短析取图最长路径。本次借鉴“改变已有排程结构”的思路；没有声称复现其禁忌搜索或析取图算法。
- **Huang、Li, A bounded two-level dynamic programming algorithm for quay crane scheduling in container terminals**。[作者所在大学全文](https://dr.ntu.edu.sg/server/api/core/bitstreams/5d477030-556b-47db-b055-21e548b3fcfa/content)。阅读第 3 节：先分配整贝作业，再调整部分工作由相邻桥吊共享，同时检查移动中的安全距离。其单向、连续分区及可移出船舶边界假设与本项目不同，不能直接套用。这里采用“先快速构造，再修复工作区段”的结构。
- **Lu、Han、Xi、Erera (2012)**。[全文](https://cedric.cnam.fr/~bentzc/INITREC/Files/CB2.pdf)。重新核对第 3 节：允许贝位抢占和自由初始停位，但移动时间依赖距离。本项目保留固定一槽移动及 S 首槽强制作业。

具体代码是针对本项目重新设计的两部分，并非上述论文的原算法复现：

1. `_best_safe_dispatch`：派工评分按桥吊相加，状态记录末台位置和是否有作业/待作业；前缀最大值保证相邻位置差至少 2。每槽 O(MN) 个状态（Python 路径元组复制另有 O(M) 因子），替代位置组合枚举。只保证当前评分最优，不保证工期最优。随机扰动改为每台贡献相加，因此随机过程与旧实现不同。
2. `_trajectory_repair`：完整排程缩短一槽后，随机替换单台桥吊一段位置轨迹，增量重算受影响的原地作业容量。所有时间边界保持安全，S 的首槽两个端点锁定。缺口可临时增大，按退火概率接受；缺口归零才输出。多余容量解码为空闲，不会多算作业。该邻域允许接续作业和双向移动，未限制连续分区。它可能停在局部最优，不提供最优性证明。

工程上，增加有限数时间参数检查、达到下界后的早停，以及独立求解进程与原子可行解检查点。直接 Python 函数仍为协作式限时；CLI 的外部进程看门狗才负责中止超时搜索。初始化目前仍枚举合法组合，极大 N、M 可能在时限内来不及产出第一解；此时报告超时，不伪造结果。

首次对照发现大工作量、3 台桥吊样例全用新 DP 时从 50 退到 51（记录在 `experiments/20260912_initial_comparison.json`）。最终 M > 3 使用 DP 与逐贝位优先级变异；M <= 3 前 300 轮用 DP，随后切回原聚焦解码器，并延长构造份额，再叠加早停和轨迹修复。两类解码器共享当前最好解；旧解码器不是第三方求解器。仍不能保证每个实例、每个随机种子都优于旧版。

少桥吊切换阶段会重置随机流与 UCB 规则学习，防止第一种解码器的奖励把第二种解码器的有效规则压制；保留第一阶段的完整最好解，最终取两阶段的较优者。检查点也只接受改进。

最终版本数据为 `experiments/20260912_final_after.json`，基线见 `experiments/20260912_comparison.json` 的 before 行，复现实验脚本为 `tools/benchmark_solver.py`。历史段落的性能和比例不再代表当前版本。

## 1. 采用的文献思想

### Lu、Han、Xi 与 Erera（2012）

论文 *A heuristic for the quay crane scheduling problem based on contiguous bay crane operations* 同时考虑可抢占贝位作业、移动时间、不可交叉和安全距离。论文的核心是：利用连续贝区分区构造无冲突排程，并把“平均活动时间下界”和“安全距离造成的局部拥挤下界”结合起来；作者还明确建议把排序、分区、重排过程作为元启发式的解码器。

- DOI：https://doi.org/10.1016/j.cor.2012.02.013
- 本项目采用：连续贝区拥挤下界、拥挤优先级、合法排程解码器；
- 本项目差异：原论文移动时间与距离相关，本项目任意距离统一为 1，因此移动下界按“首次访问不同贝位所需的最少移动次数”重新推导。

### Nguyen、Zhang、Johnston 与 Tan（2013）

论文 *Hybrid evolutionary computation methods for quay crane scheduling problems* 使用优先级驱动的排程构造过程，让表示层同时决定任务分配和执行顺序，并用局部搜索改进结果。实验显示这种“优先级表示 + 可行解码 + 局部改进”的组合适合大规模 QCSP。

- DOI：https://doi.org/10.1016/j.cor.2013.03.007
- 本项目采用：多个派工优先规则、可行性保持型解码，以及不同规则之间的自适应选择；
- 本项目差异：不调用论文中的 GA/GP 软件，本项目用纯 Python 实现轻量多起点搜索和 UCB 规则选择。

### Kaveshgar、Huynh 与 Rahimian（2012）

论文 *An efficient genetic algorithm for solving the quay crane scheduling problem* 强调 S-LOAD 初始解、压缩搜索表示以及更紧的上下界，这三者能够改善大实例的搜索效率。

- DOI：https://doi.org/10.1016/j.eswa.2012.05.091
- 本项目采用：优先选择覆盖更多正作业量且累计负荷更高的合法初始停位，并以理论下界指导搜索质量判断。

### Zhang、Zhang、Chen、Chen 与 Chen（2017）

论文 *Approximate the scheduling of quay cranes with non-crossing constraints* 给出了多桥吊非交叉调度的下界和具有最坏情况保证的分区型近似算法，说明空间分区是 QCSP 中兼顾速度与质量的重要结构。

- DOI：https://doi.org/10.1016/j.ejor.2016.10.021
- 本项目采用：保留分区型初始方案，同时允许非连续位置组合和贝位拆分来扩大搜索空间。

### Kasm 与 Diabat（2019）

论文 *The quay crane scheduling problem with non-crossing and safety clearance constraints: An exact solution approach* 使用分区启发式启动精确方法，并特别考虑减少桥吊重新定位移动。

- DOI：https://doi.org/10.1016/j.cor.2019.03.014
- 本项目采用：将移动次数作为完工时间、拆分和负荷目标之后的并列解判优指标。

### Guo、Chen 与 Wang（2012）

论文 *A hybrid generalized extremal optimization algorithm for the quay crane scheduling problem with interference constraints* 将任务—桥吊分配与可行调度解码分开，通过随机改变局部任务归属来搜索邻域，并报告这种压缩表示可以快速处理更大实例。

- 预印本：https://arxiv.org/abs/1210.6896
- 本项目采用：围绕当前最好贝位归属进行局部删除、变异和重新解码，而不是每次完全从零开始。

### 自适应大邻域搜索相关港口研究

港口综合调度研究使用“贪心初始解 + 自适应大邻域搜索 + 变邻域搜索”处理大规模和动态实例，核心是让有效的破坏/修复算子获得更多搜索机会。

- DOI：https://doi.org/10.3934/jimo.2022188
- 本项目采用：软归属继承、不同删除比例和 UCB 自适应规则选择；不使用论文中的 MIP 子问题。

### Lee 与 Chen（2010）

论文 *An improved approach for quay crane scheduling with non-crossing constraints* 提出 Best Partition（BP）动态规划和 Enhanced Best Partition（EBP）近似方案，其中 BP 可在 `O(MN²)` 结构下求较好的连续贝区划分。

- DOI：https://doi.org/10.1080/03052150902943020
- 本项目采用：用纯 Python 动态规划最小化各连续桥吊区域的最大“作业量 + 最低访问移动量”，作为固定归属解码的高质量种子；同时保留非连续分配和可拆分搜索，避免把分区近似误当成原问题的精确限制。

### Jooken、Leyman、Wauters 与 De Causmaecker（2020）

论文 *Exploring search space trees using an adapted version of Monte Carlo tree search for combinatorial optimization problems* 针对非交叉桥吊调度使用树搜索、启发式模拟、支配删除、变量域缩减和 beam width，并在基准实例上得到新的最好结果。

- 预印本：https://arxiv.org/abs/2010.11523
- 本项目采用：在时间层上保留多个部分调度状态，合并相同“剩余工作 + 桥吊位置”的状态，并用动态剩余下界和固定束宽控制内存与时间。

### Hai、Zhou 与 Xue（2026）

论文 *A Self-Adaptive Monte Carlo Tree Search Algorithm for Generalized Quay Crane Scheduling Problem* 把广义 QCSP 建模为序贯决策问题，用自适应下界进行节点选择和子树剪枝，并使用基于下界的奖励反馈搜索效果。论文面向双向移动、安全距离和不可交叉约束，强调在动态港口场景中快速给出高质量可行解。

- DOI：https://doi.org/10.1287/trsc.2025.0016
- 本项目采用：UCB 规则选择的探索系数会随“连续未改进次数”自动增大；奖励同时考虑相对理论下界的差距和本轮实际工期改进，使计算预算逐渐转向当前输入上有效的构造策略。

## 2. 本轮具体改进

1. **增强理论下界**：从原来的单贝位/总负荷下界，扩展为单贝位、总负荷、总活动时间、连续窗口拥挤、窗口作业加最低到达、可达桥吊瓶颈六部分；
2. **最低移动量下界**：设正作业贝位数为 `K`，满足强制开工的合法初始位置最多覆盖 `K0` 个正作业贝位，则所有排程至少需要 `K-K0` 次桥吊移动；
3. **连续窗口下界**：长度为 `L` 的连续贝区在安全间距 2 下最多同时容纳 `ceil(L/2)` 台桥吊，因此该贝区作业量除以最大并行度构成合法下界；
4. **拥挤优先级**：每个贝位的优先级取所有包含它的连续窗口中的最大负荷密度，引导桥吊优先处理潜在瓶颈；
5. **保留多样性**：同时运行不含拥挤项的原始规则，避免特定优先规则在某些实例上产生负作用；
6. **自适应规则分配**：根据各规则在当前实例上的历史排程质量，用 UCB 机制动态分配后续搜索次数；
7. **诚实证明状态**：达到增强下界时只声明“完工时间已证明最优”，不会把次级目标也误报为全局最优。
8. **可达性瓶颈下界**：对每个连续桥吊编号集合，统计只能由该集合处理的贝位作业与最低首次访问移动量，形成类似 Hall 条件的容量下界；
9. **分配—解码分层**：少量搜索轮次先为贝位规划唯一归属桥吊，再由安全位置组合解码；其余轮次保持可拆分作业，以免分区假设限制最短完工时间；
10. **工程时间预算**：搜索最多使用 270 秒，预留约 30 秒用于结果校验、JSON 写入、首次字体缓存和甘特图生成。
11. **聚焦转移邻域**：多数轮次只组合当前贝位、高压力未完工贝位、随机探索贝位和边界停车位；少量轮次仍完整扫描所有合法组合，在保留全局探索的同时显著增加五分钟内可评估的完整排程数。
12. **自适应大邻域式继承**：从当前最好方案继承大部分贝位归属，随机删除或变异一部分归属作为软提示，再由安全解码器修复，形成“保留好结构 + 局部跳出”的搜索。
13. **方向连续性与折返优化**：记录每台桥吊上一次移动方向，对方向反转单独计数；搜索策略同时保留零惩罚规则来守住最短工期，并加入多档折返惩罚规则，在相同工期和拆分水平下优先选择少折返、少移动方案。
14. **Best Partition 动态规划种子**：按桥吊左右顺序把贝位划成连续区域，最小化最大区域活动时间，并严格满足强制开工和桥吊可达性；该方案低频注入搜索组合，不取代自由分配搜索。
15. **精确工期可行性分支定界**：长时运行的最后约 15% 预算测试“当前最好工期减 1”是否可行；聚焦邻域只改变访问顺序，全部合法位置最终仍被枚举。相同剩余工作、位置和剩余深度的失败状态会被记忆化；若完整搜索无解即可证明当前工期最优，超时则绝不误报。
16. **限宽分层动态规划**：在精确搜索前按时间层展开状态，把相同“剩余工作 + 桥吊位置”的重复状态合并；优先保留当前作业并行度高、下一槽可立即作业且关键贝区覆盖好的状态。它只用于快速找更短上界，不用于最优性证明。
17. **三段式时间预算**：约 70% 用于多起点构造、25% 用于多样性分层搜索和三种尾部重排、最多 5% 用于有希望完成的完整分支定界；完整时间从求解函数入口计时，包含输入检查和合法位置生成。
20. **直接生成安全位置**：使用压缩坐标组合与位置偏移建立安全位置的一一映射，不再先枚举全部组合后过滤相邻位置；桥吊可达贝位也由左右安全边界直接计算。
21. **多样性与尾部重排**：分层搜索按空间位置和剩余瓶颈分组保留状态，并固定当前最好排程的不同比例前缀，联合重排所有桥吊的尾部，专门修复末段阻塞和让路问题。
22. **选择性精确证明**：仅在工期差距为 1 且合法位置数量受控时启动完整分支定界，避免复杂实例把最后预算消耗在几乎无法完成的证明搜索上。
23. **边界贝位可控共享**：连续分区种子保留不共享、放开一格边界和放开两格边界三类候选。开放贝位允许物理上可达的相邻桥吊接续作业，用于修复分区负荷不平衡，其余贝位仍使用严格归属以控制搜索空间。
24. **独立重排预算与候选角色轮换**：全局分层搜索和三档尾部重排均获得单独时间份额；多桥吊状态中按时间层轮换作业、引导、探索和停车候选，避免简单列表截断造成系统性遗漏。
25. **部分排程 MCTS**：把“剩余工作、桥吊位置、已用时间”作为树节点，用 UCB 选择部分排程，逐次展开合法位置转移并用随机化快速排程评估后果；该阶段只改进上界，不用于最优性证明。
26. **阻塞位置重排**：找到最后完成作业的桥吊，并把相邻桥吊后段的移动、等待位置作为优先重排起点；全体桥吊从该点联合重排，同时保留固定比例起点作为补充。
27. **校验补强**：独立校验器检查每台桥吊相邻时间槽的位置连续性，阻止缺少移动槽的瞬间跳转被误判为合法排程。
18. **自适应 UCB 探索**：刚出现更好排程时偏向继续使用成功规则；长时间没有改进时逐渐提高探索系数，并给真正缩短工期的规则额外奖励。
19. **绘图字体缓存**：为 Matplotlib 使用输出目录下的持久字体缓存。首次建立缓存后，相同环境再次启动可避免重复扫描全部系统字体；该优化不影响求解结果。

### Abou Kasm 与 Diabat（2019）精确两阶段框架

该论文针对不可交叉和安全距离约束，先用分区启发式快速获得可行上界，再用基于动态规划子问题的精确 Branch-and-Price 收尾，并单独优化重新定位移动。

- DOI：https://doi.org/10.1016/j.cor.2019.03.014
- 本项目采用其“快速上界 + 精确收尾”的总体结构，但精确阶段由纯 Python 状态分支定界实现，不调用列生成或外部求解器。

### Expósito-Izquierdo 等（2019）精确方法

论文 *Exact methods for the quay crane scheduling problem when tasks are modeled at the single container level* 同时考虑安全距离、不可交叉、初始位置和非零移动时间，并使用工期搜索与约束组合减少精确计算时间。

- DOI：https://doi.org/10.1016/j.cor.2018.07.005
- 本项目采用：对当前上界以下的工期做可行性搜索，并把动态下界、强制开工、合法位置域和失败状态记忆结合起来。

## 3. 示例效果

当前 `example_input.json` 的总作业量为 78，正作业贝位为 17。增强后：

```text
单贝位下界                  9
总负荷下界                 26
作业量 + 最低移动量下界     31
连续窗口拥挤下界            26
窗口作业 + 最低到达下界      31
可达桥吊瓶颈下界            31
综合下界                   31
构造排程完工时间            31
```

因此示例的完工时间 `31` 已在不使用第三方求解器的前提下得到全局最优证明。

## 4. 正确性与性能验证

- 用独立广度优先穷举对照了 80 组随机小实例（`N=3..7`）：六类下界均未超过真实最优值，所有被程序标成“已证明最优”的工期均与穷举值一致；
- 对当前 `example_input.json`，原先 `500` 次构造容易停在 `32`；加入分层状态搜索后，同样 `500` 次、30 秒预算得到 `31`，且由综合下界 `31` 证明为全局最短工期；
- 对 `N≈20` 的复杂输入，限宽搜索会按桥吊数量压缩每台桥吊的候选位置数，避免候选组合随 `M` 指数爆炸；完整搜索仍被总时间上限约束；
- 这些测试不能推出所有 `N≈20` 实例都能在五分钟内证明最优。只有输出 `MAKESPAN_OPTIMAL_BY_LOWER_BOUND` 或 `MAKESPAN_OPTIMAL_BY_EXACT_SEARCH` 时，最短工期才有严格证明；`HEURISTIC_FEASIBLE` 只表示当前最好可行解。
